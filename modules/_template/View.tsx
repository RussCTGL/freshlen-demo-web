// 📋 TEMPLATE — your page content. Must be the DEFAULT export.
// Everything you build lives inside this modules/<slug>/ folder.

export default function View() {
  return (
    <section className="space-y-4">
      <p className="text-sm text-gray-500">
        Replace this with your workstream demo. Keep all files inside your module folder.
      </p>
    </section>
  );
}
